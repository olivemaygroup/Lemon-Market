const addReview = async (ctx) => {};

const myReviews = async (ctx) => {};

const editReview = async (ctx) => {};

const deleteReview = async (ctx) => {};

export default { addReview, myReviews, editReview, deleteReview };
